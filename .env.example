# Passcode required to upload (set this in Vercel Project Settings -> Environment Variables)
UPLOAD_PASSCODE=

# Automatically provided by Vercel when you create/connect a Blob store —
# you don't need to set this manually in Vercel, only for local dev if needed.
# BLOB_READ_WRITE_TOKEN=
